{
  "devDependencies": {
    "@playwright/test": "^1.20.1"
  },
  "scripts": {
    "debug": "npx playwright test --headed --timeout=0 --workers=1"
  },
  "dependencies": {
    "dotenv": "^11.0.0",
    "genson-js": "^0.0.8"
  }
}
